export const config = {
    apiKey: 'create config.secrets.js and set the values there',
    authDomain: 'create config.secrets.js and set the values there',
    databaseURL: 'create config.secrets.js and set the values there',
    projectId: 'create config.secrets.js and set the values there',
    storageBucket: 'create config.secrets.js and set the values there',
    messagingSenderId: 'create config.secrets.js and set the values there'
};

export const LogRocketID = '';

export const gaID = '';